import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />,  document.getElementById('root'));
 /*it only renders one component!
  index js 에서 html 처럼 사용을 해야됨 component 같은 경우*/
